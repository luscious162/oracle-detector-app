# Web Framework and Extensions
Flask>=2.3.3,<2.4
Flask-Cors>=4.0.0,<5.0

# WSGI Server (for Render deployment)
gunicorn

# Core ML and Image Processing
numpy>=1.26.4,<2.0
opencv-python>=4.8.0,<4.9  # 或者 opencv-python-headless
tensorflow>=2.18.0,<2.19 # TFLite Interpreter 需要这个包

# 其他您的应用直接导入的库 (例如 base64, datetime 等标准库无需列出)
# ...

# 注意: Keras 已移除，因为我们现在使用 TFLite Interpreter
